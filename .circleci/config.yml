version: 2
jobs:
  build:
    docker:
      - image: circleci/openjdk:8-jdk
    steps:
      - checkout
      - run: mvn cobertura:cobertura org.eluder.coveralls:coveralls-maven-plugin:report -DrepoToken=$COVERALL_TOKEN
