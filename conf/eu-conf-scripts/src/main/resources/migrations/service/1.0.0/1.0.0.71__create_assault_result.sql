CREATE TABLE T_ASSAULT_RESULT (
  ID         BIGINT NOT NULL AUTO_INCREMENT,
  DICE INTEGER,
  ROUND_LOSS INTEGER,
  THIRD_LOSS INTEGER,
  MORALE_LOSS INTEGER,
  FIRE BIT,
  BESIEGER BIT,
  PRIMARY KEY (ID)
);

