CREATE TABLE T_COMBAT_RESULT (
  ID         BIGINT NOT NULL AUTO_INCREMENT,
  `COLUMN` VARCHAR(255),
  DICE INTEGER,
  ROUND_LOSS INTEGER,
  THIRD_LOSS INTEGER,
  MORALE_LOSS INTEGER,
  PRIMARY KEY (ID)
);

