package com.mkl.eu.front.client.map.component.menu;

/**
 * Description of file.
 *
 * @author MKL.
 */
public enum ContextualMenuItemTypeEnum {
    /** Type Text, no action. */
    TEXT,
    /** Default type: a label and an action. */
    ITEM,
    /** Type submenu, with a label, no action. */
    SUB_MENU,
    /** Type separator, no label, no action. */
    SEPARATOR;
}
