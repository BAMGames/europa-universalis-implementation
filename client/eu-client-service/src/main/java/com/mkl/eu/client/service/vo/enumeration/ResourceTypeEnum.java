package com.mkl.eu.client.service.vo.enumeration;

/**
 * Enumeration for type of resources.
 *
 * @author MKL.
 */
public enum ResourceTypeEnum {
    /** Products of orient. */
    PO,
    /** Spices. */
    SP,
    /** Products of America. */
    PA,
    /** Sugar. */
    SU,
    /** Sugar (of Brazil). */
    SUBR,
    /** Slaves. */
    SL,
    /** Cotton. */
    CO,
    /** Cotton (of Asia). */
    COAS,
    /** Fur. */
    FUR,
    /** Fish. */
    FISH,
    /** Salt. */
    SALT,
    /** Silk. */
    SILK,
    /** Wood. */
    WOOD
}
