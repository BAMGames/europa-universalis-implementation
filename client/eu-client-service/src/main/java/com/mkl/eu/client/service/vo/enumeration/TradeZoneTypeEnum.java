package com.mkl.eu.client.service.vo.enumeration;

/**
 * Enumeration for type of trade zone.
 *
 * @author MKL.
 */
public enum TradeZoneTypeEnum {
    /** Sea trade zone (STZ = ZM). */
    ZM,
    /** Country trade zone (CTZ = ZM). */
    ZP
}
