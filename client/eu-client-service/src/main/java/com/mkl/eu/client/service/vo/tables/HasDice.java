package com.mkl.eu.client.service.vo.tables;

/**
 * Interface for objects that has a die roll (often in tables).
 *
 * @author MKL.
 */
public interface HasDice {
    /** Modified die roll. */
    Integer getDice();
}
