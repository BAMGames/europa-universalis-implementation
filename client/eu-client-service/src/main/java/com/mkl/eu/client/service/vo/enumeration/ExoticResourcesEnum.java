package com.mkl.eu.client.service.vo.enumeration;

/**
 * Enumeration of exotic resources (Cotton, silk, wood,..).
 *
 * @author MKL
 */
public enum ExoticResourcesEnum {
    /** Exotic resource spice. */
    SP,
    /** Exotic resource sugar. */
    SU,
    /** Exotic resource fish. */
    FISH,
    /** Exotic resource products of america. */
    PA,
    /** Exotic resource silk. */
    SILK,
    /** Exotic resource salt. */
    SALT,
    /** Exotic resource cotton. */
    CO,
    /** Exotic resource furs. */
    FUR,
    /** Exotic resource products of orient. */
    PO,
    /** Exotic resource slave. */
    SL,
    /** Exotic resource wood. */
    WOOD
}
