package com.mkl.eu.client.service.vo.enumeration;

/**
 * Enumeration for winner of battle.
 *
 * @author MKL.
 */
public enum BattleWinnerEnum {
    /** If the winner is the phasing side. */
    PHASING,
    /** If the winner is the non phasing side. */
    NON_PHASING,
    /** If there is no winner. */
    NONE
}
